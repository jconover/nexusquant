# mcp

MCP server. Wraps the signal, risk, executor, watchlist, and slack services
behind a typed Model Context Protocol tool surface — the only interface the
agent has to the platform. **Phase 0: stub with `/healthz` and `/readyz`
only.**
